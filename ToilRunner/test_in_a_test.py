import openmc
import openmc_data_downloader
from pxr import Usd

print('this has worked!')

# Managed to run this and the py_test_docker_compose.py in a running docker container :) now to test if we can run a workflow from this